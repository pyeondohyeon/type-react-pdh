import React from "react";
import Logo from "../public.logo";
import HaderNav from "./nav";

const PublicHeader = () => {
    return <header>
        <Logo />
        <HaderNav />
    </header>
}

export default PublicHeader